# cognee-onto

An open-source Cognee integration package for the Onto API.
Includes a custom `onto_intake_task` Pipeline Task that hooks in before `cognify` to clean raw HTML URLs into token-efficient Markdown and applies epistemic trust scoring (0-100).

Also contains custom `DataPoint` ontologies for "The Hangover" memory graph.
